# Психологический чат-бот: архитектура и сценарии

## 1. Диалоговые сценарии и мультимедиа

### 1.1 Ежедневные чекины
- **Цель:** фиксировать настроение, сон, уровень стресса, план на день.
- **Триггер:** запланированное напоминание через webhook или команда пользователя.
- **Ход диалога:**
  1. Приветствие + эмпатичный вопрос о самочувствии.
  2. Быстрые кнопки: `😀 Отлично`, `🙂 Нормально`, `😟 Так себе`, `😢 Плохо`.
  3. Уточняющий вопрос в зависимости от выбора (например, при низком настроении — просьба описать ситуацию; при хорошем — план на день).
  4. Завершение: подтверждение записи и предложение совета/ресурса.
- **Мультимедиа:**
  - Аудио/видео дыхательных упражнений при высоком стрессе.
  - GIF/изображения с простыми растяжками при низкой энергии.
  - Карусель карточек (если платформа позволяет) с подсказками по распорядку сна.

### 1.2 Напоминания
- **Типы:** медикаменты, записи к терапевту, выполнение практик.
- **Ход диалога:**
  1. Бот сообщает напоминание и предлагает варианты: `Выполнил`, `Напомнить позже`, `Пропустить сегодня`.
  2. При выборе «Напомнить позже» — запрос интервала (кнопки `15 мин`, `1 час`, `Вечером`).
  3. При выполнении — подтверждение, похвала, предложение записать ощущения.
- **Мультимедиа:**
  - Короткие клипы/аудио с инструкциями (например, как принять медитационную позу).
  - PDF/изображения с чек-листом упражнений.

### 1.3 Советы и микро-интервенции
- **Триггеры:** пользователь запрашивает помощь («мне тревожно»), бот распознаёт эмоциональный спад.
- **Ход диалога:**
  1. Бот уточняет ситуацию (кнопки «Тревога», «Грусть», «Паника», «Бессонница»).
  2. Для выбранной темы — подбор техники (дыхание, Журнал мыслей, прогрессивная релаксация).
  3. Выдача пошаговой инструкции + медиаконтент (видео, аудио, инфографика).
  4. Предложение сохранить технику в избранное или запланировать повтор.

### 1.4 Экстренная поддержка
- **Цель:** быстро оценить риски, предоставить ресурсы.
- **Ход диалога:**
  1. Распознавание триггерной фразы или сильного негатива.
  2. Немедленное сообщение о серьёзности ситуации и готовности помочь.
  3. Динамические варианты: `Поговорить с оператором`, `Показать контакты экстренных служб`, `Техники самопомощи`.
  4. Если пользователь выбирает оператора — передача на живого специалиста (webhook-уведомление + пометка в CRM).
  5. Логирование события как «критичного», при необходимости отправка push/e-mail ответственным.
- **Мультимедиа:**
  - Быстрые аудиозаписи с дыханием 4-7-8.
  - Карточки с кризисными номерами и инструкциями, оптимизированные для быстрого чтения.

## 2. Webhook-инфраструктура и состояние пользователя

### 2.1 Supabase Functions (Edge)
- **Endpoints:** `/webhook/telegram`, `/webhook/vk`, `/webhook/whatsapp` (в зависимости от каналов).
- **Структура:**
  - Edge Function принимает входящий апдейт → нормализует → кладёт в очередь (Supabase Queue или встроенный `realtime` канал).
  - Обработчик вызывает сервис `ConversationEngine` (TypeScript):
    - Загружает состояние пользователя (`profiles` + `conversation_state` таблицы в Supabase Postgres).
    - Применяет state machine (XState или кастомная FSM) для определения шага диалога.
    - Генерирует ответ + набор `actions` (сохранение эмоции, расписание напоминаний, пересылка медиаконтента).
  - Результат отправляется через API мессенджера.
- **Хранение состояния:**
  - `conversation_state` — JSONB с последним сценарием, шагом, контекстом.
  - `user_settings` — предпочтения (часовой пояс, желаемые напоминания, язык).
  - `checkins` — история настроений и заметок.
- **Планировщик:** Supabase Scheduled Functions для напоминаний — вызывают `/reminders/dispatch` и генерируют исходящие сообщения.

### 2.2 Отдельный сервер (Node.js/Fastify)
- **Когда выбрать:** нужна гибкая интеграция (например, с Amplitude SDK, ML-моделями), высокая нагрузка.
- **Компоненты:**
  - Fastify сервер с маршрутами `/webhook/:channel`, `/actions/*`, `/health`.
  - Redis для краткосрочного состояния/очередей (failover и rate limiting).
  - PostgreSQL (Supabase, Neon) для долговременных данных.
  - Worker (BullMQ) обрабатывает тяжёлые задачи: генерация отчётов, синхронизация с аналитикой.
- **State flow:**
  1. Вебхук → нормализация → `ConversationService.processUpdate`.
  2. Получение/создание `UserSession` (Redis + fallback в Postgres).
  3. Выполнение сценария (FSM / диалоговый движок).
  4. Запись логов в `dialog_events` (Postgres) + стрим в аналитический пайплайн.
  5. Отправка ответа, постановка follow-up задач (напоминания, уведомления терапевту).

## 3. Динамические клавиатуры, быстрые действия и логирование

### 3.1 Динамические клавиатуры
- **Telegram:** использовать reply_keyboard и inline_keyboard.
  - Пример: чек-ин → `keyboard: [["😀 Отлично", "🙂 Нормально"], ["😟 Так себе", "😢 Плохо"]]`.
  - Inline-кнопки для отложенных напоминаний (`callback_data: {action:"remind", delay: "PT15M"}`).
- **VK/WhatsApp:** адаптировать под соответствующие API (template buttons, reply choices).
- **Генератор клавиатур:** модуль `KeyboardFactory` принимает `ScenarioState`, возвращает JSON для канала.

### 3.2 Быстрые действия
- **Сохранённые ответы:** хранить в `quick_actions` (тип, локаль, payload).
- **Контекстные подсказки:** после логирования стресса → предложить «Запланировать дыхание» (кнопка с `action=plan_breathing`).
- **UI в приложении:** если есть webview, показывать меню с карточками действий (React/Vue компонент, данные через REST `/quick-actions`).

### 3.3 Логирование диалогов и эмоций
- **Структура таблиц:**
  - `dialog_events`: `id`, `user_id`, `channel`, `timestamp`, `type` (user_message/bot_message/system), `payload` (JSON), `emotion_score`.
  - `emotion_snapshots`: `id`, `user_id`, `source_event_id`, `model` (ручной ввод или ML), `label` (радость/грусть/тревога/нейтрально), `score`.
  - `checkin_records`: агрегированные данные (настроение, заметки, триггеры).
- **Пайплайн эмоций:**
  - Если платформа поддерживает — кнопки настроения → прямое значение.
  - Для свободного текста — отправка в sentiment/affective API (например, Google Cloud NL, HuggingFace модель), результат сохраняется.
- **Анонимизация:** хранить чувствительные данные с шифрованием (pgcrypto) и редактировать/удалять по запросу пользователя.
- **Мониторинг:** дашборды Supabase/Metabase для отслеживания динамики эмоций и вовлечённости.

## 4. Аналитика (Amplitude/Segment)

### 4.1 События
- `checkin_started`, `checkin_completed`, `reminder_sent`, `reminder_completed`.
- `advice_requested`, `advice_completed`, `crisis_triggered`, `handoff_to_agent`.
- `quick_action_used`, `media_played`, `emotion_logged`.

### 4.2 Интеграция через Segment
- Серверный трекер (Node.js):
  ```ts
  import Analytics from '@segment/analytics-node';
  const analytics = new Analytics({ writeKey: process.env.SEGMENT_WRITE_KEY });

  analytics.track({
    userId: user.id,
    event: 'checkin_completed',
    properties: {
      mood: payload.mood,
      channel: update.channel,
      scenario: state.scenario,
    }
  });
  ```
- Forwarding в Amplitude, Mixpanel, BigQuery для глубокой аналитики.
- Использовать `analytics.identify` для установки трейтов (`timezone`, `risk_level`, `therapy_stage`).

### 4.3 Прямое подключение Amplitude
- Если Segment не нужен, вызывать Amplitude HTTP API (`/2/httpapi`).
- Добавить middleware `AnalyticsService.log(event)` внутри сценариев.
- Обеспечить ретраи при сетевых сбоях (экспоненциальный backoff, очередь).

### 4.4 Метрики и панели
- **Retention:** % пользователей, завершивших ≥3 чекина за неделю.
- **Conversion:** доля пользователей, которые после напоминания отмечают «Выполнил».
- **Crisis response time:** среднее время от `crisis_triggered` до `handoff_to_agent`.
- **Media engagement:** CTR по кнопкам, сколько пользователей прослушали аудио/видео.
- Использовать Cohort/Path анализа для оптимизации сценариев.

## 5. Безопасность и комплаенс
- Логирование в отдельную таблицу `audit_log` для действий операторов и критичных событий.
- Маскирование PII в аналитике (хранить только user_id, без имён).
- Поддержка GDPR/локальных норм: механизмы удаления данных, экспорт истории диалога по запросу.
- Ограничение доступа к таблицам настроек и эмоций через RLS (Supabase) или отдельные роли в Postgres.

## 6. Следующие шаги реализации
1. Сформировать JSON-схему состояния пользователя и настроить миграции БД.
2. Подготовить Supabase Edge Function с нормализацией входящих апдейтов и базовой FSM.
3. Реализовать модуль динамических клавиатур (Telegram/VK) и quick actions.
4. Настроить логирование эмоций + интеграцию с аналитикой через Segment.
5. Провести нагрузочное тестирование и аудит безопасности.
